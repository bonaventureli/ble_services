0.1版本增加特征值，可以读写数据，串口可以打印手机发送的数据，手机APP使用nRF Connect. 苹果机测试
0.4 功能全部跑通，包括CMD状态反馈，但是没有办法接收串口模拟数据。
计划下一个版本取消不必要的注释，想办法加入串口接收函数。
0.5 取消了部分调试打印信息，修复cmd处理数据前后加延时

0.8版本首次flash调试ok。
0.9版本rtt打印正常，uart发送正常，接收还在调试,RX可以接收数据.
1.0dubug 
flash ok
uart tx,rx ok
cmd ok

1.0release
flash ok
uart ok
cmd ok
cmd return ok

1.1Debug
加入配对代码，但是没有相应的APP测试
1.2Debug
加入OTA DFU部分，但是不能回复到bootloader部分，目前分析，可能是存储密钥的flash地址和dfu的地址0x78000冲突了。

1.3Debug
取消flash，OTA DFU正常 增加UART-DFU.zip，代码没有变动

1.4Debug
修改flash地址0x70000之后，OTA DFU正常工作

1.5Debug
增加rssi

1.6Debug
增加CRC16校验

1.7Debug
修改flash地址
size:0x5A00->0x5200
注释：Handle_Rssi()
resp_pair_request()

1.8Debug
增加了转义

1.81Debug
验证crc

1.9Debug
加入simple.proto
